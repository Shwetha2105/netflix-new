// API Configuration
// Change this to your deployed backend URL after deploying the server
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';

export default API_BASE_URL;
